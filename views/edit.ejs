<% include partials/header %>

<div class="container">
	<div clas="row">
		<h1 id="add-header">Edit "<%= art.title %>"</h1>
			<div id="add-form">
				<form action="/art/<%= art._id %>?_method=PUT" method="POST">
					<div class="form-group">
						<input class="form-control" type="text" name="title" value="<%= art.title %>">
					</div>
					<div class="form-group">
						<input class="form-control" type="text" name="image" value="<%= art.image %>">
					</div>
					<div  clas="form-group">
						<input class="form-control" type="text" name="description" value="<%= art.description %>">
					</div>
					<div  clas="form-group">
						<input class="form-control" type="text" name="artUrl" placeholder="<%= art.artUrl %>">
					</div>
					<div class="form-group">
						<button id="add-button" class="btn btn-lg btn-primary btn-block">Submit</button>
					</div>
				</form>
			</div>
	</div>
</div>

<% include partials/footer %>